#include <stdio.h> 
public void swaping(int x, int y)
{
    x=x+y;
    y=x-y;
    x=x-y;
    printf("\nValue of x is %d and y is %d",x,y);
public void swaping(int x, int y);
}
    int main(){
        int a,b;
        printf("Enter two values : ");
        scanf("%d %d",&a,&b);
    
return 0;
}